import turtle
window = turtle.Screen()
window.title ("polygon")
window.bgcolor("pink")

board = turtle.Turtle()

for i in range(3):
    board.forward(100)
    board.left(120)

turtle.done()